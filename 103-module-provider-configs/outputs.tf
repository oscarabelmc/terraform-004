output "inherited_provider_note" {
  value = "Module implicitly inherits default provider from parent."
}

output "explicit_provider_note" {
  value = "Module received provider via `providers` argument in module block."
}
