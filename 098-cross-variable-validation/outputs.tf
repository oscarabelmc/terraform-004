output "validation_note" {
  value = "Variable validation blocks are evaluated before plan generation. Cross-variable validation fails during input evaluation, preventing any plan from being produced."
}
